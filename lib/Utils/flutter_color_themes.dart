import 'package:flutter/material.dart';

class AppColors extends MaterialColor {
  const AppColors(super.primary, super.swatch);

  static const containerBG = Color(0xFFFEF8EE);
  static const gradientStart = Color(0xFFFC7902);
  static const gradientEnd = Color(0XFFC62E00);
  static const GlobalBG = Color(0XFFFFEFD9);
}
